export interface Tours{
    tourTitle: string,
    destination: string,
    duration: string,
    price: string,
    tourType: string,
    tourImage: File,//not sure if its file or string
    shortDescription: string
}

export interface Tours{}